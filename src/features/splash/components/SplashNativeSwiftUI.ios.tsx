import { SplashFallback } from './SplashFallback';

// Temporarily keep the installed Development Build on the stable JS fallback.
// The native animated implementation remains available for a future rebuild.
export default SplashFallback;
