/**
 * Mock de integração com o Waze.
 * Futuramente, a URL poderá ser construída usando o esquema `waze://`.
 */
export function openInWazeMock(): void {
  // Integração futura: abrir uma URL `waze://` com endereço ou coordenadas.
}

/**
 * Mock de integração com o Apple Maps.
 * Futuramente, a URL poderá usar `http://maps.apple.com/`.
 */
export function openInAppleMapsMock(): void {
  // Integração futura: abrir uma URL `http://maps.apple.com/` com coordenadas.
}
